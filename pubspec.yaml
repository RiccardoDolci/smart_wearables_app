name: smart_wearables_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_reactive_ble: ^5.2.0
  permission_handler: ^11.1.0
  syncfusion_flutter_charts: ^31.2.3
  path_provider: ^2.1.4
  share_plus: ^10.1.4
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true